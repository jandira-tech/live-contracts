[project]
name = "sec-listener"
version = "0.1.0"
description = "Continuous RSS listener for SEC filings with EX-10 exhibit extraction"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp",
    "datamule",
]
